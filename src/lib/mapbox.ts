// Paste your Mapbox public token here (starts with "pk.").
// Get one free at https://account.mapbox.com/access-tokens/
// Public tokens are safe to commit — they're scoped to map tile usage.
export const MAPBOX_PUBLIC_TOKEN = "";
